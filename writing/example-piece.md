# Example Piece

The first line or two of the piece, shown in the index listing.

---

This is where your writing goes. You can use all standard Markdown:

Paragraphs are separated by blank lines.

## A section heading

More text here. *Italics* and **bold** work as expected.

---

For poetry, line breaks are preserved with `white-space: pre-line` in the CSS —
just write each line on its own line in the Markdown file.
